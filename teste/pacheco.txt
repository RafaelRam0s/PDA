Funções que eu preciso que vc faça, trantando nas Models, Services e DAO:

    CadastrarPaciente(
        String Nome,
        String Foto (vou guardar uma url),
        Datetime Data_de_nascimento,
        String Sexo,
        String Endereco,
        String Telefone,
        String Forma_de_pagamento
    ) return bool (true para criado e false para deu erro)

    CadastroEspecialidade(
        String PK Indentificador,
        String Nome
    ) return bool (true para criado e false para deu erro)

    CadastroConsulta(
        String FK Indentificador_paciente,
        String FK Indentificador_medico,
        Datetime Data_hora_consulta
    ) return bool (true para criado e false para deu erro)

    CadastroMedico(
        String PK Indentificador,
        String Nome,
        String Numero_CRM,
        String Endereco,
        String Telefone,
        String FK Indentificador_especialidade
    ) return bool (true para criado e false para deu erro)

    CadastroExame(
        String PK Indentificador,
        String Nome,
        DECIMAL(10,2) Valor,
        String Orientação (uns 2000 caracteres),
        String FK Indentificador_paciente,
        String FK Indentificador_medico,
        Datetime Data_hora_exame
    ) return bool (true para criado e false para deu erro)

    VerMedicos() return com os campos "Indentificador | Nome | CRM | Endereço | Telefone | Especialidade ", traga de volta em um array

    VerMedico(String indentificador_medico) return apenas com o campo nome

    VerConsultasDoMedico(String Indentificador_medico) return com os campos "Indentificador Consulta | Indentificador Paciente | Nome Paciente | Data e hora para realização", traga de volta em um array

    VerExamesDoMedico(String Indentificador_medico) return com os campos "Indentificador Exame | Indentificador Paciente | Nome Paciente | Data e hora para realização", traga de volta em um array

    VerExamesDoSistema() return com os campos "Indentificador | Nome | Valor | Orientação | Paciente | Médico | Data", traga de volta em um array

    VerExame(String Indentificador_exame) return com os campos "Indentificador | Nome | Valor | Orientação | Paciente | Médico | Data"

    VerPacientes() return com os campos "Indentificador | Nome", traga de volta em um array

    VerPaciente(String indentificador_paciente) return apenas com o campo nome

    VerConsultasDoPaciente(String Indentificador_paciente) return com os campos "Indentificador Consulta | Indentificador Médico | Nome Médico | Data e hora para realização", traga de volta em um array

    VerExamesDoPaciente(String Indentificador_paciente) return com os campos "Indentificador Exame | Indentificador Médico | Nome Médico | Data e hora para realização", traga de volta em um array
